import React from 'react';
import LiveClasses from '../LiveClasses';

const LiveClassesView: React.FC = () => {
    return (
        <div>
            <LiveClasses title="Browse NEET LIVE Classes" />
        </div>
    );
};

export default LiveClassesView;